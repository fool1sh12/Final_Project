<%= simple_form_for book do |form| %>

  <%= form.input :title %>

  <div>
    <%= form.association :author, label_method: :full_name %>
  </div>

  <div>
    <%= form.input :description %>
  </div>

  <div>
    <%= form.submit :class => "btn btn-info" %>
  </div>
<% end %>
